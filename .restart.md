# Relational Substrate Restart Notes

Last updated: 2026-06-24

## Tooling and Process (added 2026-06-13/14 — Claude, from the 2026-06 review)

Process recommendations in `docs/process-recommendations.md` are now runnable
tooling. All additive — none touch `src/model.js` or any research conclusion.

- `npm run model:sensitivity` (`analysis/model-sensitivity-analysis.mjs`, #5) —
  toy-model robustness: how the coherent region depends on the gate
  thresholds, metric weights, and inputs. Finding: the `closureStress < 0.62`
  cutoff is near-inert; the `identityScore >= 0.62` cutoff drives the gate.
- `npm run evidence:ledger` (`analysis/evidence-ledger.mjs`, #7) — one
  consolidated, auditable cross-track ledger. Reads the regenerated benchmark
  summary + the task-map declared numbers, mechanically enforces the
  guardrails (EM → one line, boundary orientation-only, declared counts match
  computed, conventional comparator present), and FLAGS declared-vs-computed
  drift. Run after `npm run reports`.
- `npm run gate:check` (`analysis/predeclaration-gate.mjs`, #1/#2) — HARD gate
  for new scoring work: `predeclare({target,modelForm,descriptors,heldOut})`
  then `requireScoringGate()`; throws on unregistered descriptors or a missing
  conventional baseline. Its self-test also runs inside `npm run guardrails`.
- `npm run case:run -- case.json` (`analysis/run-exported-case.mjs`, #8) —
  replays a case exported from the browser sandbox's "Export case (JSON)"
  button through `calculateOutcome`; closes the visual → batch loop.
- `npm run guardrails` now also runs the gate self-test, and the new analysis
  files are in its syntax-check list.

**Confidence reconciliation (2026-06-13).** The task-map Current Status
numbers had drifted high vs the project's own regenerated computation;
reconciled DOWN to computed: inferential convergence 6.25 → 5.6, cross-domain
5.7 → 5.2, unification 5.2 → 4.8, checks-passed 187/192 → 190/202. (6.25 was
the cap, not the computed value; it stepped down after the heteroatom-torsion
trans-side failures.) `npm run evidence:ledger` now reports zero drift.

Only tooling/docs were added; the large pre-existing uncommitted research
worktree was left untouched. Recommendation #4 (keep `model.js` transparent if
extended) is guidance-only. Retrofitting the existing RI scoring steps to call
the predeclaration gate is deliberately NOT done — the gate is the required
path for NEW scoring work.

## Where We Are

The active front is the measured material refractive-index gate. The broad benchmark remains unresolved. Current generated inferential convergence is 5.6/10 and should stay below the 6.25/10 cap until a measured material-property prediction passes without endpoint fitting.

The RI diagnostic chain currently completes 34 steps:

```bash
node analysis/ri-refractive-index-chain-runner.mjs
```

Latest generated runner:

- `analysis/out/ri-refractive-index-chain-runner.md`

Torsion repair front (2026-06-15):

- The disulfane and H-Se-Se-H same-topology transfers remain failed validation rows: both still fail trans-side topology/magnitude under the locked comparison scripts.
- A diagnostic-only repair candidate now exists: `trans_planar_topology_penalty`.
- Script: `npm run diagnostic:heteroatom-trans-repair`
- Diagnostic result: one predeclared trans-planar penalty repairs the already-exposed disulfane / selenane trans rows, but this is calibration pressure only and carries no evidence weight.
- Required next gate: reserve one fresh H2X2-like or adjacent heavy-atom rotor target before lookup; freeze formula, coefficient, geometry, charge rules, grid, and pass/fail criteria before consulting it.
- status: all RI chain steps completed
- latest step: `ri-structural-model-form-predeclaration.mjs`
- current handoff: structural RI model form is predeclared, but target-specific scoring remains blocked until a clean source-locked held-out material/polytype is reserved

Peroxide observable-route front (2026-06-24):

- TBHP cleared the frozen qualitative observable-route comparator under a pre-lookup reservation.
- Evidence-ledger review classifies this as limited branch support only: no global confidence change and no energy-model promotion.
- The TBHP trans route saturates the clamped selection distance at 1, while the source barrier values are computed effective-potential comparators with experimental support for the torsional construct.
- Required next gate: predeclare a fresh peroxide-family target with a non-saturated intermediate route distance and a quantitative energy or barrier criterion before source lookup.
- That gate was executed on peroxyformic acid under reservation hash `6f5dee57741c435e`.
- The source-locked ground-state profile uses 0, 30, and 90 degree C-O-O-H coordinates; the 30 degree row gives the required unsaturated selection distance.
- Result: held-out intermediate-profile failure. Source energy rises away from the 0 degree equilibrium, while the live absolute-angle cis-crowding term predicts decreasing penalties.
- Current model-form boundary: do not reserve another peroxide target under this combined route/energy form. First predeclare a crowding descriptor relative to source-defined equilibrium topology and pressure-test it only on exposed cases.
- Equilibrium-relative crowding v2 was predeclared under hash `62bb2cd510659cb4` with no fitted coefficients.
- Exposed feasibility passes H2O2, TBHP, and peroxyformic acid. Peroxyformic Spearman is 1.0 and normalized-rise MAE is about 0.1046; H2O2 cis/trans ratio remains within the frozen factor-2 gate.
- This is not recovered evidence. The original absolute-angle form remains falsified; v2 now requires one fresh pre-lookup target reservation and unchanged quantitative profile scoring.
- HMHP was reserved for that gate under hash `b723c3eb66e045cd`.
- HMHP source lock failed cleanly: the primary study reports three optimized conformers with multiple changing dihedrals and changing intramolecular hydrogen-bond donor topology, not one isolated three-coordinate O-O torsional scan.
- No HMHP score was produced. Next step is metadata-only screening for a source that explicitly exposes a one-dimensional torsional profile before another identity is reserved.
- That screen selected methyl chloroperoxide as an adjacent-family boundary target, reserved under hash `d0291f9dae41f452`.
- Source lock failed because accessible primary metadata does not expose numerical scan rows, relative energies, relaxation rules, or signed torsion convention.
- No CH3OOCl score was produced. The peroxide v2 fresh-validation branch is now source-blocked; do not continue reserving metadata-only identities without an accessible numerical one-dimensional profile.
- The secondary absolute-torsion track was tightened from hydroxylamine to exact-topology neutral hydrazine and reserved under hash `3447add3ae0e0690`.
- Neutral-hydrazine source lock failed: NIST exposes one aggregate experimental barrier, while the 2012 two-barrier computed PES is not accessible with enough angle/stationary-point mapping to populate the frozen rows without analyst choice.
- No neutral-hydrazine score was produced. The next unblocked track is measured roughness BRDF/PSD, not another torsion reservation.
- The conventional Rayleigh-Rice PSD-backed BRDF shape model was predeclared under hash `342f0e3cb174d64d`.
- The first-listed tungsten specimen in Le Bohec et al. (2024) was reserved under hash `585a3f59d1b60487`.
- The full tungsten paper was recovered from Strathprints. M100 is the frozen first specimen, but its angular BRDF trace has no stated wavelength, so it cannot be paired with the independently measured PSD under the frozen Rayleigh-Rice mapping.
- A replacement-source screen selected Tayabaly et al. (2013), and the first eligible optical-flat target was reserved under hash `2bc047ecca02cdc6`.
- Full-text inspection locks the polished silicon wafer, 488 nm, 5 degree incidence, and the valid region above 6 cycles/mm. However, the paper exposes BRDF-derived `PSDscatter`, not raw angular BRDF rows, so the original scorer remains blocked.
- A separate independent-profile-PSD versus BRDF-derived-PSD model form was therefore predeclared under hash `ad3892b700d0b82f`. Tayabaly is exposed feasibility only; the next step is synthetic known-answer implementation before a fresh target screen.
- While blocked, a reusable PSD-to-BRDF shape scorer was implemented with log-log interpolation inside measured PSD support, no extrapolation, frozen specular masking, rank/MAE/area checks, and target-hash enforcement.
- Synthetic known-answer validation passes: a PSD-derived BRDF passes exactly and an inverse-shape BRDF fails. This is implementation readiness only, not evidence.
- Raw wavelength-tagged angular BRDF data can be entered through `analysis/roughness-tungsten-source-data.example.json` and scored with `npm run benchmark:roughness-psd-brdf-run -- <source-data.json>`.

Profile-PSD vs scatter-derived-PSD comparator v0 (2026-06-24, second front):

- This is the second roughness model form, hash `ad3892b700d0b82f`, created because Tayabaly exposes BRDF-derived PSDscatter rather than raw angular BRDF rows. It compares an independently measured surface-height profile PSD against the source-reported PSDscatter directly in spatial-frequency space. Shape only; no substrate evidence.
- The comparator is now implemented: `analysis/roughness-profile-psd-scatter-psd-model.mjs` (`scoreProfilePsdVsScatterPsdShape`). It log-log interpolates the independent profile PSD at each surviving PSDscatter frequency, drops below/above-support and source-excluded rows (no extrapolation), median-centers both curves in log10, and enforces the frozen pass/fail set: >=8 surviving rows, >=0.5 decade span, Spearman >=0.8, centered-log10 MAE <=0.25 dex, 90th-pct |residual| <=0.60 dex, and >=80% adjacent slope-sign agreement.
- Synthetic known-answer validation passes: `npm run benchmark:roughness-psd-cross-feasibility` (`analysis/roughness-profile-psd-scatter-psd-feasibility-diagnostic.mjs`). A median-offset-only matching curve passes with ~1e-16 centered error and Spearman 1; an inverse-shape curve fails with Spearman -1; below-support and excluded rows are dropped. This is implementation readiness only, not evidence.
- Both files are in the `npm run guardrails` syntax-check list. Input contract for a future locked surface is documented in `analysis/roughness-profile-psd-scatter-psd-source-data.example.json` (synthetic template; two PSD curves must come from independent modalities for the same specimen state).
- Deliberately NOT yet built: a scoring case runner for this form. Per the discipline, a fresh paired surface must be metadata-screened and reserved before any numerical lookup; the runner (which is the scoring path) should be added together with that target reservation so scoring stays gated behind it.
- Metadata source screen executed: `npm run benchmark:roughness-psd-cross-source-screen` (`analysis/roughness-profile-psd-scatter-psd-source-screen.mjs`). Abstract/index level only, no curve extraction. Selected candidate is the Duparre et al. (2002) Applied Optics 41, 154-171 same-sample round-robin (DOI 10.1364/AO.41.000154): 15 samples measured by AFM/Talystep/confocal topography AND angle-resolved scattering, with 2D PSDs computed from all to allow direct comparison — an independent topography PSD plus a same-sample scattering-derived PSD. Schroeder et al. (2011) Opt. Express 19, 9820 is the deferred fallback; Harvey 2009 (moderately-rough method paper) and the Optik 2023 joint-inversion PSD are rejected as wrong construct. Tayabaly stays barred (exposed feasibility only).
- Open requirements flagged before any numerical lookup of Duparre 2002: the scattering wavelength/geometry is NOT in the abstract and must be locked from full text (this is the same wavelength gap that sank Le Bohec for Form 1); one single sample and one single topography instrument must be frozen as the independent-PSD source; and the topography/scattering PSD overlap band must be confirmed to expose >=8 matched rows over >=0.5 decade.
- Duparre 2002 target reserved: `npm run benchmark:roughness-psd-cross-reserve` (`analysis/roughness-duparre-profile-psd-scatter-psd-target-reservation.mjs`), reservation hash `5305a937566e8b24`, serving model-form hash `ad3892b700d0b82f`. It freezes a source-ORDER selection rule before any numerical lookup: take the first sample in source presentation order exposing both an independent topography PSD and an angle-resolved-scattering-derived PSD; the independent PSD comes from the first instrument in the frozen precedence AFM -> mechanical profiler (Talystep) -> confocal; the comparison curve is the ARS-derived PSD; use the source-stated scattering wavelength/geometry. Only the abstract has been seen; sample list, wavelength, and curves are still unknown.
- Source-lock attempted and BLOCKED on access: `npm run benchmark:roughness-psd-cross-source-lock` (`analysis/roughness-duparre-profile-psd-scatter-psd-source-lock-screen.mjs`), status `source-lock-blocked-full-text-not-accessible`. The three required full-text facts (sample presentation order / first sample, stated ARS wavelength + geometry, and a verified topography/scatter PSD overlap band with >=8 rows over >=0.5 decade) are not exposed by the abstract; the Optica full text is paywalled and the ResearchGate full text returns HTTP 403. Per discipline, nothing was fabricated — no sample identity, wavelength, or curve value asserted (explicitly NOT laundering the search-engine material-list summary or assuming 632.8 nm).
- Required next gate: obtain VERIFIED full text of Duparre 2002 (institutional Optica access or the Fraunhofer IOF PDF), confirm the three facts, and source-lock against reservation hash `5305a937566e8b24`; only then build the scoring case runner (enforcing that hash) and extract numerical rows. If Duparre 2002 stays inaccessible, the Schroeder et al. 2011 fallback is now CLOSED for this comparator: checked 2026-06-24 (abstract/PubMed), it is measured-ARS vs modeled-ARS with the topography PSD as the model input — no independent scatter-derived PSD curve — so it does not fit Form 2 (it is a Form-1 candidate only, and only on its smooth subset with metallic-surface optical-constant handling). The real fallback is a fresh metadata screen for another same-specimen independent-topography-PSD plus scatter-derived-PSD source.
- Open-access source located, read, and recorded: `npm run benchmark:roughness-psd-cross-sulc-record` (`analysis/roughness-sulc-open-access-feasibility-record.mjs`). Sulc et al., Coatings 13, 1853 (2023), DOI 10.3390/coatings13111853, pulled via Unpaywall -> Brno VUT DSpace (MDPI itself is Akamai bot-blocked to curl/WebFetch). Cached at `analysis/source-cache/sulc-2023-coatings-13-1853.pdf` (+.txt; gitignored). Construct CONFIRMED: four rough-silicon samples, AFM topography PSDF (Gwyddion) vs multi-wavelength ARS-recovered PSDF, overlaid in Fig 5; wavelengths 457.9/514.5/647.1 nm, normal incidence; scatter-PSD support region II k=0.0005-0.012 nm^-1 (~1.38 decades, > 0.5).
- Šulc 2023 is now EXPOSED/feasibility-only (its Table 3 + Fig 5 were read during accessibility verification, so per the Tayabaly rule it cannot be fresh held-out validation). Form-2 scoring on it is still BLOCKED: the scatter-derived PSDF is numerically reconstructable from Table 3 {sigma_II, l1..l6, tau} + fixed nodes, but the independent AFM PSDF is figure-only (Table 3 gives only scalar AFM sigma_II/sigma_T; Data Availability = "upon request"). Systematic obstacle: across this AFM+ARS literature the independent topography PSDF is published as a figure, not numerical rows.
- Sibling lead: Šustek et al., J. Opt. 23, 105602 (2021), DOI 10.1088/2040-8986/ac1f35 (ref [5], same group/construct, single wavelength) — NOT inspected (metadata-only check preserved its held-out status) but CLOSED-access (Unpaywall: no OA copy), so needs institutional access.
- Live blocking point on the whole roughness track: numerically scoring Form 2 needs AFM PSD rows that this literature publishes only as figures. Concrete unblocks: (a) request the Sulc 2023 AFM PSD data from the corresponding author (then run the frozen comparator as an exposed-source feasibility demo only), or (b) get institutional access to the un-inspected Šustek 2021 (or the reserved Duparre 2002) for fresh held-out evidence. The comparator + validation + screen + reservation + Sulc feasibility record are all complete and committed.
- Whole roughness PSD/scatter track now has a consolidated runner: `npm run benchmark:roughness-psd-scatter-chain` (`analysis/roughness-psd-scatter-chain-runner.mjs`). It regenerates both comparator lineages (Form 1 raw-BRDF, Form 2 profile-vs-scatter) in narrative order, 13 steps, and reads back each step's status into one table (predeclarations frozen, both scorers known-answer-pass, tungsten/tayabaly/duparre locks failed-or-blocked-as-expected, Sulc recorded). Execution success only; asserts no evidence.
- Open-data search for a tabulated both-curves source: negative. A Zenodo dataset search (plus the open-literature screen) found no source exposing BOTH an independent topography PSD and a scatter-derived PSD as numerical rows for one specimen; the on-topic hits are roughness-value comparisons, not the PSD pair. Conclusion: the figure-only publication of the independent PSD is a field-wide obstacle, so numerically scoring Form 2 needs author-provided/supplementary PSD data, not just more searching.

## Current Boundary

The active RI families are no longer candidate repairs:

- `baseline density + Al/O + Al-comp response`: reference diagnostic only
- `split Ca scaffold family`: quarantined outside source domain
- `split Ca/Mg scaffold family`: quarantined outside source domain

Do not score further material-RI targets under those families as candidate repairs. They can be reported only as historical/reference diagnostics.

Current decision from `analysis/out/ri-ca-scaffold-model-form-quarantine-diagnostic.md`:

```text
do not score further material-RI targets under baseline, split-Ca, or split-Ca/Mg as candidate repairs; use them only as reference diagnostics until a new source-locked model form is predeclared
```

## How We Got Here

Key late-chain states:

- Larnite, `Ca2SiO4`, was classified before scoring as compact/lower-coordinate non-melilite Ca scaffold.
- Larnite split-Ca prediction failed: predicted `1.75034` vs measured midpoint `1.719666`, error `0.03067`.
- Larnite baseline pass was local only: predicted `1.71769`, error `0.00198`.
- Merwinite, `Ca3Mg(SiO4)2`, broke the baseline density-control pass: baseline predicted `1.69837` vs measured `1.713666`, error `0.0153`.
- Merwinite split-Ca and split-Ca/Mg passed, but that is not promotable because Mg chemistry is confounded.
- Spurrite, `Ca5(SiO4)2(CO3)`, was reserved and scored as Mg-removed/carbonate-introduced control contrast.
- Spurrite failed all frozen families:
  - baseline `1.64084`, error `0.02299`
  - split-Ca `1.68693`, error `0.0231`
  - split-Ca/Mg `1.69584`, error `0.03201`
- Wollastonite is deferred: CIF candidates exist, but optical/density rows are broad or secondary and not polytype-locked.
- Pure Ca-Si-O source screen found no new source-locked held-out target:
  - wollastonite/pseudowollastonite source-blocked
  - hatrurite has `n = n.d.`, `D(meas.) = n.d.`, `D(calc.) = n.d.`
  - larnite/rankinite/kilchoanite already exhausted

## Most Important Files

Start here:

- `docs/research-task-map.md`
- `docs/model-status-parameter-map.md`
- `docs/ri-structural-pattern-note-2026-05-16.md`
- `analysis/ri-refractive-index-chain-runner.mjs`
- `analysis/ri-structural-model-form-predeclaration.mjs`
- `analysis/ri-ca-scaffold-model-form-quarantine-diagnostic.mjs`
- `analysis/ri-pure-ca-si-o-source-exhaustion-screen.mjs`
- `analysis/external-ri-spurrite-confound-substitution-validation.mjs`
- `analysis/ri-wollastonite-polytype-source-lock-screen.mjs`
- `analysis/external-ri-merwinite-density-control-validation.mjs`
- `analysis/external-ri-larnite-ca-scaffold-transfer-validation.mjs`

Generated outputs live under `analysis/out/`. Treat them as generated; source of truth is `analysis/*.mjs` plus the tracked docs.

## Next Viable RI Work

Do not do another frozen-family target score.

### 2026-06-24 update: predictor form predeclared + a real wollastonite source candidate found

- The RI predictor EQUATION + protocol is now predeclared: `npm run benchmark:ri-predictor-predeclare` (`analysis/ri-structural-predictor-form-predeclaration.mjs`), hash `b764feb54971f5a9`. It is a FIXED-PARAMETER, structurally-resolved Lorentz-Lorenz predictor (no fitted coefficients; parameters from named published refractivity/polarizability tables locked before the target). The test: does structure-resolved refractivity (Ca-O coordination + bridging/non-bridging O split + T-site polarizability) beat a composition-only Lorentz-Lorenz control on a fresh held-out mineral? Pass = |n_struct - n_meas| <= 0.010 AND <= 0.70 * |n_conv - n_meas|. Readiness only; nothing scored.
- A candidate that may finally unlock the long-deferred wollastonite source lock: `npm run benchmark:ri-wollastonite-baita-candidate` (`analysis/ri-wollastonite-baita-source-candidate.mjs`). Marincea, Dumitras, Hatert et al., Crystals 16(4), 247 (2026), DOI 10.3390/cryst16040247 — gold open access, cached via Liege ORBI at `analysis/source-cache/marincea-2026-wollastonite-1a-baita.pdf`. Same-sample, explicit polytype wollastonite-1A, with measured alpha/beta/gamma = 1.616/1.629/1.631, measured density Dm = 2.922(3), and a crystal-structure determination. This is exactly the polytype-locked optical+density+structure pairing the May screen said was missing; it postdates that screen (2026 paper).
- Two conditions before reserving/scoring it (NOT decided here): (a) composition-purity call — the formula (Ca1.000Mg0.002Mn0.001Fe0.001)(Al0.004Ti0.001Si0.994)O3 has ~0.2-0.5% trace substitution; the prior "no Mg/Fe/Mn" rule targeted structural substitution, not trace, so this needs an explicit programme decision; (b) lock the predictor parameter tables (Shannon-type dielectric polarizabilities; bridging vs non-bridging O refractivities) independently of the target value (abstract alpha/beta/gamma already seen, so parameters must be canonical published constants, not chosen to fit the answer).
- Important: only abstract-level facts have been used; the paper's structural/optical TABLES have NOT been extracted, so a clean reserve -> parameter-lock -> read -> score sequence is still available. Do that as the next careful step.
- Parameter source-lock done (and it changed the plan): `npm run benchmark:ri-predictor-parameter-lock` (`analysis/ri-predictor-parameter-source-lock.mjs`). Locked source = Shannon & Fischer 2016, American Mineralogist 101, 2288 (DOI 10.2138/am-2016-5730), open MSA preprint, cached at `analysis/source-cache/shannon-fischer-2016-polarizabilities-preprint.pdf`. Locked values: Anderson-Eggleton relation (c=2.26, eqn 4a, literal form to transcribe from source); coordination-resolved alpha(Ca2+) = [5]1.910 [6]1.790 [7]1.670 [8]1.570 [9]1.480 [10]1.390 [11]1.320 [12]1.250 Angstrom^3; alpha(Si4+,[4])=0.284; O2- anion-volume model alpha-(O2-)=1.79, No=-1.776 (eqn 6).
- TWO findings from the parameter lock: (1) FORM INCONSISTENCY — predictor v0 (b764feb54971f5a9) predeclared Lorentz-Lorenz + a bridging/non-bridging O split, but the only accessible authoritative source uses Anderson-Eggleton + an anion-volume O2- model. Coordination-resolved cations DO match. So do NOT score under v0; emit a reconciled predictor v1 (Anderson-Eggleton + anion-volume O2- + coordination-resolved cations, literal eqns transcribed) before any target lookup. (2) STRATEGIC — Shannon-Fischer 2016 is already a complete, validated structural RI predictor (~2600 compounds), so an RS predictor built on these parameters would largely recapitulate established crystal chemistry; a wollastonite pass would be a CONSISTENCY check, not independent evidence for the topology grammar, and should not by itself raise unification confidence. This sharpens docs/information-limitation-assessment-2026-06-24.md.
- Predictor v1 built (consistency check): `npm run benchmark:ri-predictor-v1-predeclare` (hash `9f8838307cc1a70d`, supersedes v0) + model `analysis/ri-structural-predictor-v1-model.mjs` + `npm run benchmark:ri-predictor-v1-feasibility`. Equations transcribed VERBATIM from the cached SF2016 preprint pp.5-8 (read visually, not from memory): Anderson-Eggleton (4a) alpha=(n^2-1)Vm/(4pi+(4pi/3-c)(n^2-1)) with c=2.26; inverse (4b) n=sqrt(4pi*alpha/((c-b)alpha+Vm)+1), b=4pi/3; additivity (5); anion-volume O2- (6) alpha=alpha0*10^(-No/Van^nexp). Contrast frozen: structural = coordination-resolved alpha(Ca by CIF CN) + anion-volume O2- vs conventional = coordination-agnostic alpha + fixed free-ion O2-. Pass = |n_struct-n_meas|<=0.010 AND <= |n_conv-n_meas|. Explicitly framed as a CONSISTENCY CHECK (must not raise unification confidence).
- v1 feasibility result: FULLY VALIDATED AGAINST THE SOURCE. Status `v1-machinery-validated-and-oxygen-anchored`. AE round-trip recovers n to 2e-16; additivity exact; eqn (6) oxygen model sign-corrected to alpha=alpha0*10^(No/Van^n) and exponent reconciled to the source-stated nexp=1.20. The earlier n=1.0 confusion was a MIS-PAIRING: the source's 1.44 is dense SiO2 (stishovite, small Van), not quartz. With nexp=1.20 the model reproduces TWO source-stated (compound, Van, alpha) anchors -- quartz SiO2 (Van=18.8 -> 1.586 vs 1.58, 0.4%) and BaO (Van=42.5 -> 1.710 vs 1.71, 0.0%). Oxygen params: alpha0=1.79, No=-1.776, nexp=1.20.
- v1 is now NO LONGER PARAMETER-BLOCKED. The full predictor (AE + additivity + anchored anion-volume O2- + coordination-resolved cation table) is validated against Shannon-Fischer 2016.

### 2026-06-24: FIRST held-out RI score executed — wollastonite-1A consistency check PASS

- Operator accepted the trace composition as pure-enough CaSiO3. Target reserved: `npm run benchmark:ri-wollastonite-baita-reserve` (hash `6a05937a1040ae8c`, serves v1 `9f8838307cc1a70d`), freezing the recipe + the conventional control (agnostic alpha(Ca)=1.5475, free-ion O2-=1.79) BEFORE extraction.
- Structural data source-locked from the cached Marincea 2026 PDF: cell V=396.91 A^3, Z=6 -> Vm=66.15, Van=22.05 A^3; Ca six-fold (CN=6 -> alpha=1.79); Si [4]; single SiO3 chains. Vm cross-checks density to 0.2%.
- Score: `npm run benchmark:ri-wollastonite-baita-score`. n_meas=1.62533; n_struct=1.62814 (abs err 0.00281, <=0.010 PASS); n_conv=1.65276 (err 0.02743). Value-added gate PASS (struct beats structure-blind control ~10x). Both gates pass -> CONSISTENCY CHECK PASS.
- CONFIDENCE STANCE (deliberate, do not "fix"): confidence numbers are UNCHANGED (inferential 5.6 / cross-domain 5.2 / unification 4.8; evidence ledger still 6/6, no drift). This pass is consistency-only: predictor v1 IS the Shannon-Fischer 2016 method, and the structural improvement is dominated by the established anion-volume O2- correction, so it confirms wollastonite-1A is consistent with established polarizability crystal chemistry but is NOT independent evidence for the topology grammar. Per the v1 predeclaration + docs/information-limitation-assessment, a consistency pass does not move confidence. The cap-release condition ("a measured material-property prediction passes without endpoint fitting") is met MECHANICALLY, but the prediction is not RS-specific evidence, so the cap stays and the unification thesis stays unconfirmed. This is a genuine open question for the operator: whether/how to record a consistency-only held-out pass in the programme narrative (recommendation: record it as a methodological consistency result, not a confidence increase).
- Next viable RI work after this: if independent grammar evidence is wanted, design a test where the RS reading predicts something SF2016 does NOT (genuinely hard); otherwise RI is effectively answered by established crystal chemistry and effort is better spent elsewhere.

### 2026-06-24: sharper SF2016-independent RI test designed (`npm run benchmark:ri-anisotropy-test-design`)

- `analysis/ri-anisotropy-topology-test-design.mjs` works the "option 1" question rigorously. SF2016 is a SCALAR model, so the only observables it cannot reach are (a) optical ANISOTROPY (birefringence magnitude, optic sign, indicatrix orientation) and (b) the additivity-rule deviation Delta.
- Predeclared the one viable falsifiable RS hypothesis: RS topological-anisotropy rule v0 (DESIGN, not scored). H1: high-index axis aligns with the dominant silicate-connectivity route (chain axis for inosilicates; sheet normal = low-index for phyllosilicates), within 30 deg. H2: birefringence ranks by connectivity dimensionality (sheet 2D > chain 1D > framework 3D; orthosilicate low). Falsification + held-out protocol frozen.
- HONEST CONCLUSION (the key finding): within the scalar index, RS is captured by SF2016, so RI is effectively a CLOSED domain for novel RS evidence at the scalar level. The anisotropy opening is real and predeclared but is NOT a clean discriminator -- it competes with the established bond-polarizability tensor model (overlapping predictions), and pursuing it REQUIRES building RS anisotropy theory (new descriptors connectivity_dimensionality + indicatrix_orientation that RS does not have). So a sharper RI test exists, but RI cannot give DECISIVE independent grammar evidence.
- STRATEGIC DECISION for the operator (open): build RS anisotropy (uncertain payoff, still not decisive) vs accept RI is closed and seek a domain where the grammar makes a prediction with NO established competitor. This extends docs/information-limitation-assessment-2026-06-24.md.

### 2026-06-24: grammar order-effect thread (the no-established-competitor search)

- Scoping memo `docs/grammar-native-prediction-scoping-2026-06-24.md`: diagnosed why no novel evidence appears (the programme tests a coherence-OVER-HISTORY grammar on STATIC observables that mature theory already predicts; on those benches the grammar is calibrated, not derived). The grammar's one distinctive claim (from model.js simulateSequence + memory carry) is PATH-DEPENDENT identity persistence; its sharp falsifiable fingerprint is a DIRECTIONAL ORDER EFFECT (gentle-first protects), which static theories and order-INDEPENDENT cumulative baselines (Miner's rule) cannot predict.
- P1 derivation `npm run grammar:order-effect` (`analysis/grammar-order-effect-derivation.mjs`): drove simulateSequence with reversed regime schedules at matched multisets, 192 configs. Result: the model produces a consistent order effect whose direction depends on the endpoint. FINAL-STATE = recency (end-gentle wins, 100%); WHOLE-HISTORY (min identity / did it ever break) = PRECONDITIONING (gentle-first better in 95.8%, 11 survival flips, 0 reverse). So the grammar's distinctive prediction is REAL in the model but ONLY on a whole-history endpoint -> any real test must use cycles/time-to-first-failure, NOT residual strength (which is recency-dominated, opposite sign).
- P2 scoping `npm run grammar:order-effect-p2-scoping` (`analysis/grammar-order-effect-p2-data-scoping.mjs`): accessible fatigue coaxing data CONFIRMED -- Ti-6Al-4V ELI (Data in Brief / PMC5458061), Harvard Dataverse doi:10.7910/DVN/SUCU5X, CC0 1.0. Both L-H (gentle-first) and H-L (harsh-first) two-block tests with the SAME amplitude pairs (matched -> isolates order), 25 tests, cycles-to-failure available. L-H.zip cached at `analysis/source-cache/tialloy-LH.zip` (H-L.zip = Dataverse file id 3008844, not yet downloaded). Honest framing: a directional pass (L-H life > H-L life) corroborates the grammar vs the order-independent Miner null (the only clean no-competitor claim) but does NOT beat established nonlinear-damage models (Corten-Dolan etc.) which also predict the sequence effect; decisive evidence needs a magnitude prediction (grammar->life calibration, calibration-tainted).
- P2 EXECUTED (2026-06-25): `npm run grammar:order-effect-fatigue-test` (`analysis/grammar-order-effect-fatigue-test.mjs`). Used Table 1 from PMC5458061 (extracted from PMC HTML; xlsx tooling absent and not needed). Found a CLEAN data-internal test avoiding the total-cycle confound: the dataset's D-labeled H-L tests give virgin Nf(0.010,Reps=-1)=2048 (512/0.25=1024/0.50=1536/0.75=2048, all consistent). The linear-damage (Miner) null forbids a residual block-2 life above virgin (residual=Nf*(1-D1)<=Nf); the grammar (coaxing/gentle-first protects) predicts residual>virgin. RESULT: 4/4 gentle-first (L-H) tests ending at 0.010 have residual high-amplitude life {2823,2161,3209,2316} > 2048 (mean ratio 1.283, +28%). PREDICTION CONFIRMED vs the linear-damage null -- the first real-data corroboration of a distinctive RS-grammar prediction.
- Honest limits (all recorded): coaxing is well documented (sign expected for any memory model); does NOT beat established nonlinear cumulative-damage models (Corten-Dolan etc.) which also predict it; only the gentle-first side is cleanly testable here (harsh-first residual-reduction side needs Nf(0.006), web lookup was rate-limited; both H-L.zip and L-H.zip are now cached in source-cache if per-specimen Miner sums are wanted later); n=4, one material, one endpoint. CONFIDENCE UNCHANGED (5.6/5.2/4.8; ledger 6/6 no drift) -- directional corroboration vs the Miner null is not decisive over nonlinear models; decisiveness needs a magnitude prediction (grammar->life calibration, calibration-tainted).
- P2 STRENGTHENED to both sides (2026-06-25): `npm run grammar:order-effect-fatigue-miner` (`analysis/grammar-order-effect-fatigue-miner-test.mjs`). Got the companion constant-amplitude S-N (Carrion & Shamsaei, Data in Brief 7 (2016), PMC4761652 Table 1; Nf=2Nf/2, replicate means): Nf(0.012)=1284, Nf(0.010)=2048, Nf(0.008)=6813, Nf(0.007)=24747, Nf(0.006)=76769, Nf(0.005)>1.36M runout. TWO independent validations: Nf(0.010)=2048 matches the D-label derivation, and Nf(0.006)=76769 = 2x the L-H first block 38384 (confirms D1=0.5 design). Computed full Miner sums D for all two-block tests: gentle-first L-H beneficial (D>1) in 3/3 (D=1.717, 2.962, 1.718); harsh-first H-L detrimental (D<1) in 3/4 (0.682, 0.820, 0.922; the lone exception 0.008->0.006 D=1.18 is adjacent-amplitude, a known nuance); matched {0.006,0.010} pair D(L-H)=1.72 > 1 > D(H-L)=0.82. The order-INDEPENDENT Miner null (D=1) is violated in the grammar's predicted direction on BOTH sides, on real validated S-N data. PREDICTION CONFIRMED both-sided.
- Status of the grammar thread: a distinctive RS-grammar prediction (path-dependent / order effect, derived in P1) has now been confirmed against real fatigue data on both the beneficial (gentle-first) and detrimental (harsh-first) sides, beating the order-independent Miner null. Still NOT decisive over nonlinear cumulative-damage models (which also predict the sequence effect); CONFIDENCE UNCHANGED (5.6/5.2/4.8; ledger 6/6). The only remaining path to possible decisiveness is the MAGNITUDE test: calibrate grammar memory-carry to a fatigue-life scale and compare predicted D(L-H)/D(H-L) magnitudes to a nonlinear-damage model on held-out pairs -- a real modelling project with attribution risk, to weigh against the honest verdict that the grammar may be a conceptual lens.

Legitimate next paths:

1. Convert the predeclared structural RI model form into a target-specific scoring plan only after source lock.
   - Current predeclared substrate-facing descriptors: Ca coordination, mean Ca-O, T-site polarizability, and silicate connectivity.
   - Predictor equation/protocol now predeclared (hash `b764feb54971f5a9`); the conventional comparators are density and Lorentz-Lorenz, control evidence only.
   - Still required before scoring: composition-purity decision, predictor parameter source-lock, target reservation, pre-score CIF classification, and the frozen pass/fail tolerance above.

2. Unlock a clean held-out target source.
   - Need optical constants, measured density, and paired CIF for one material/polytype identity.
   - Wollastonite remains attractive only if a primary polytype-specific optical/density row is found.
   - Pseudowollastonite is only usable if a source-grade optical/density row plus paired structure row is found.
   - Do not use broad pooled rows or secondary aggregate WebMineral-style rows as held-out targets.

3. Move to another validation track.
   - Hydrazine cation torsion: qualitative/ratiometric pass but absolute-magnitude miss remains.
   - Roughness/interface: need measured BRDF/PSD/broadband scatter, not more smooth-surface scalar TIS.
   - Lower priority EM track should remain lower priority until material-property or held-out torsion improves.

## Guardrails

- Keep current confidence capped; current generated inferential convergence is 5.6/10 and must not be raised above 6.25/10.
- Do not count EM-02 through EM-17 as independent domains; they are one electromagnetic evidence line.
- Do not count boundary phase as core evidence; it is orientation-only.
- Do not count target-implied material-property coefficients as validation until they clear a fresh held-out target.
- Do not count `trans_planar_topology_penalty` as torsion evidence until it clears a fresh post-redesign held-out target unchanged.
- Do not promote split-Ca or split-Ca/Mg from merwinite.
- Do not treat spurrite as pure Ca-Si-O validation; carbonate is introduced.
- Do not reuse rankinite, kilchoanite, larnite, merwinite, or spurrite as fresh validation targets.

## Commands To Rehydrate

Check syntax on the latest work:

```bash
node --check analysis/ri-ca-scaffold-model-form-quarantine-diagnostic.mjs
node --check analysis/ri-structural-model-form-predeclaration.mjs
node --check analysis/ri-pure-ca-si-o-source-exhaustion-screen.mjs
node --check analysis/external-ri-spurrite-confound-substitution-validation.mjs
node --check analysis/ri-refractive-index-chain-runner.mjs
```

Run the chain:

```bash
node analysis/ri-refractive-index-chain-runner.mjs
```

Broad project commands from the task map:

```bash
node --check analysis/material-nbo-stoichiometry.mjs
node --check analysis/external-material-refractive-index-challenge.mjs
node --check analysis/external-benchmark-summary.mjs
node --check analysis/milestone-external-review-summary.mjs
node --check analysis/claude-drift-completeness-review.mjs
npm run reports
npm run build
```

Parameter experiment pickup:

```bash
npm run model:parameter-experiments
```

Current result: strongest average identity lift is `reseat +0.10`, then `route +0.10`, then `scatter -0.10`; strongest failure probes are `reseat -0.10`, `route -0.10`, and phase away from `0.5`.

Custom recipe runner:

```bash
npm run model:recipe -- analysis/parameter-recipe.example.json
```

The recipe runner emits a deterministic recipe hash plus `analysis/out/parameter-recipe-*.lock.json`; use that lock file if a recipe is ever promoted from exploration toward a predeclared target test.

## Claude Helper

Claude Opus is available through the `cello` user environment. Use narrow prompts and avoid broad claims.

Invocation pattern:

```bash
printf '%s' 'PROMPT' | sudo -i -u cello claude -p --model opus --permission-mode bypassPermissions --add-dir /home/cello/domains/Relational-Substrate
```

Good use cases:

- ask for wording cautions on a specific artifact
- review one or two generated outputs
- rank next artifact choices under current guardrails
- ask for source-lock criteria

Avoid broad prompts that ask it to solve the whole research programme.

## Dirty Worktree Note

The worktree has many existing modified and untracked files. Do not revert unrelated changes.

Use this status command because of safe-directory ownership:

```bash
git -c safe.directory=/home/cello/domains/Relational-Substrate -C /home/cello/domains/Relational-Substrate status --short
```
